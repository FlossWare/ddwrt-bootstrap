# DD-WRT Bootstrap Configuration
#
# Sourced by dd-wrt.sh and debian.sh at startup.
# Override these defaults for your router's storage layout.

# USB mount point where dd-wrt/, entware/, and debian/ directories live
STORAGE_DIR="/tmp/mnt/sda1"

# Log file for bootstrap output
LOG_FILE="/tmp/flossware.log"
